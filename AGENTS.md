# 语言和风格
- 始终使用简体中文回复
- 回答问题时不要客套话，尊重事实

# 求真原则
- 不确定/信息不足时,询问而非猜测
- 对环境/配置/源码/行为的结论必须有证据
- 回答里把"事实"和"推测"分开写

# 核心原则
## 开发哲学
- **KISS**: 选择最简单有效的解决方案
- **YAGNI**: 只实现当前明确需要的功能
- **SOLID**: 确保代码设计合理，职责清晰
- **渐进式迭代**: 保持每次改动可编译、可验证
- **务实态度**: 优先满足真实需求而非理想化设计
## Linus 三问原则
在任何分析前必须问自己：
1. "这是真实问题还是想象的问题？" — 拒绝过度工程
2. "有更简单的方法吗？" — 总是寻求最简单的解决方案
3. "这会破坏什么？" — 向后兼容是铁律

# 通用编码规范
统一风格，代码可读性好，可维护性强。
- 注释：所有函数、方法，必须要有中文注释，描述意图、逻辑等；类属性必须添加完整类型注解，禁止使用 Any（极特殊场景除外）
- python代码：所有函数、类、工具必须包含 Google 风格 Docstring，明确说明功能、入参、返回值、异常场景。
- python工具链：使用 uv 统一管理 Python 版本与依赖，禁止使用 pip、venv、poetry 等其他工具管理版本和依赖。
- 禁止使用 print 输出日志。
- 统一错误码体系：业务错误、系统错误、权限错误等分类明确。
- java代码：需要遵守阿里巴巴Java开发规范。
- 包含 api、key等配置项，禁止硬编码到代码里，需要在配置文件中配置。
- 如果实现与设计不同，需要同步修改文档，不要只写代码不同步修改文档。
- 其他在本规范之外的最佳实践。

# 可运维性规范
- 健康检查：每个服务必须提供 /health 健康检查接口，返回服务状态、依赖可用性。
- 部署说明：每个模块必须包含 README.md，说明启动方式、配置说明等。
- 监控埋点：核心链路（检索、工具调用、模型推理）必须埋点耗时、成功率指标等。

# 运行方式
- 本项目进入到 backend 目录才能执行 uv 操作，不要在外面等目录直接执行（无效操作）